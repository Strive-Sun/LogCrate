## 1. 多卷调度

- [ ] 1.1 记录当前 C:\ 与 D:\ 串行全量重建基准和状态计数（MFT 串行/并行各 3 次及单卷完整阶段已记录；缺少修改前完整应用级样本）
- [x] 1.2 抽取可独立执行的 NTFS 卷枚举、恢复和 USN 追赶任务结果
- [x] 1.3 实现受索引服务并发上限约束的卷级后台 worker 和完成通道
- [x] 1.4 在首次重建与启动恢复流程中复用并行调度器
- [x] 1.5 由单一协调器按卷完成顺序合并 Tantivy、SQLite 与 USN 快照

## 2. 状态、取消与错误隔离

- [x] 2.1 分离每卷操作总耗时、当前阶段耗时和完成冻结耗时，并保留真实阶段、发现记录数与可搜索文件数
- [x] 2.2 使单卷失败只降级对应卷，其它卷继续索引
- [x] 2.3 确保暂停、清除和重建等待旧 generation 的所有卷 worker 退出
- [x] 2.4 所有卷查询快照达到终态后 2 秒内发布全局 `ready`，持久化结束后执行最多 4 批或 500 毫秒的事件交接并移交常驻 worker
- [x] 2.5 更新中英文 UI，使首批 MFT 记录到达前显示实际准备阶段，之后按卷和全局汇总显示真实计数与正确耗时
- [x] 2.6 使全局发现数和可搜索数始终由各卷快照求和，并增加数据库计数一致性诊断

## 3. 增量恢复性能保护

- [x] 3.1 在 USN 变化应用前区分普通文件变化与目录变化
- [x] 3.2 普通文件变化继续使用按文件快速路径；目录变化需要全图处理时自动切换对应卷的全量 MFT 重建
- [x] 3.3 为 MFT 枚举、路径解析、查询索引合并、持久化和事件交接增加独立阶段日志与记录数
- [x] 3.4 确保策略切换、事件交接和后续增量处理均在后台线程运行，UI 线程不承担扫描或索引工作
- [x] 3.5 查询索引达到可搜索目标后立即发布卷及全局就绪状态，后台持久化不得继续显示为正在建立索引
- [x] 3.6 目录变化发生在持久化窗口时保存未完成快照并交给 watcher 对账，禁止再次进入全图 USN 路径

## 4. 验证

- [x] 4.1 增加有界卷 worker、并发 IPC 容量和逐卷完成状态刷新测试
- [x] 4.2 增加阶段切换计时、完成耗时冻结、跨卷计数聚合以及“不误报 0 MFT”的前后端测试
- [x] 4.3 增加持续产生文件事件时全局仍能有界进入 `ready`、且事件无损移交给常驻 worker 的测试
- [x] 4.4 增加普通文件 USN 快速路径、目录变化全量降级、单卷失败、取消重建和共享数据库无锁冲突的回归测试
- [x] 4.5 运行完整 Rust 测试、Clippy、格式和前端测试
- [x] 4.6 在相同 C:\ 与 D:\ 范围执行至少 3 次并行全量重建和目录变化恢复，形成修改前后 `benchmark.md`
- [ ] 4.7 验证全部卷可搜索中位耗时低于串行基线、目录变化恢复不超过对应卷全量重建基准的 120%、首批结果回退不超过 10%、UI 输入响应 p95 不超过 100 毫秒，且最终结果数与状态提示一致
- [x] 4.8 在约 590 万条 MFT 基准规模上验证：调度 ≤2 秒、单卷 MFT ≤20 秒、首批结果 ≤30 秒、全部卷可搜索 ≤60 秒、状态收敛 ≤2 秒、后台持久化 ≤5 分钟，并记录 MFT 与查询索引吞吐率
- [x] 4.9 定位并修复 Windows 重建完成阶段偶发的“拒绝访问（os error 5）”：为查询索引目录切换补充阶段和路径错误上下文，验证句柄释放/有界重试，并覆盖活动索引、并发查询、遗留 `.next`/`.previous` 及失败回滚测试
