## 1. 后端拖入检查与监控接口

- [x] 1.1 抽取可复用文件分类逻辑，并新增 `inspect_dropped_file` 命令返回规范化路径、类型、实际监控路径、日志属性和监控覆盖状态
- [x] 1.2 增加通过指定绝对路径添加监控根的前端 API，复用现有 `add_watch_dir` 去重、持久化和 watcher 初始化
- [x] 1.3 增加 zip、日志后缀、内容采样文本、任意普通文件、目录、失效路径和已覆盖监控路径 Rust 测试
- [x] 1.4 扩展检查契约以接受任意普通文件和文件夹，返回实际监控路径与是否为文本日志
- [x] 1.5 规范化监控根去重：已覆盖目标不重复添加，新增父级根时归并已有子级根并释放冗余 watcher

## 2. 原生拖放交互

- [x] 2.1 订阅并正确清理 Tauri `onDragDropEvent`，处理 enter/leave/drop 和串行 guard
- [x] 2.2 处理原生 enter/over/leave/drop 事件，接收行为不依赖 drop 坐标
- [x] 2.3 单路径检查通过后按 directory/archive/file 分流；多路径和不支持的特殊类型整体拒绝并提示
- [x] 2.4 实际监控路径未受覆盖时通过指定路径 API 添加监控，原路径不产生文件变更
- [x] 2.5 添加监控后刷新/定位 zip；已受监控时跳过重复添加
- [x] 2.6 拖入文件夹时将其自身作为监控根，任意普通文件使用父目录
- [x] 2.7 移除拖入监控确认弹窗，检查成功后自动持久化监控并保持删除确认弹窗不变
- [x] 2.8 移除拖入悬停和处理中的全窗口提示层，保持原生 drop 静默处理

## 3. 裸日志自动打开

- [x] 3.1 拖入裸文本日志后立即复用现有查看会话打开内容，不等待监控添加完成
- [x] 3.2 确保添加失败和已受监控均不关闭已打开日志，打开失败沿用现有错误清理
- [x] 3.3 防止拖入任务覆盖删除确认、更新弹窗或并发打开状态
- [x] 3.4 父目录已受监控或添加成功后逐层定位、选中并滚动到拖入的裸日志，且不重复打开查看会话
- [x] 3.5 当前查看日志不匹配后缀筛选时仍保持目录节点可见，确保定位完成后清理展开链

## 4. 测试与文档

- [x] 4.1 增加拖入事件分流、单路径限制、自动监控和裸日志自动打开时序的前端纯逻辑测试
- [x] 4.2 在 Windows 验证资源管理器拖入文件夹、任意文件、zip/.log/.txt、自动监控、监控去重、日志定位和大日志打开，并由 macOS CI 验证编译测试
- [x] 4.3 运行 Rust fmt、Clippy、测试以及前端格式、测试、lint 和生产构建
- [x] 4.4 在 `CHANGELOG.md` 的 `Unreleased` 章节逐条记录拖入监控和裸日志自动打开行为
- [x] 4.5 增加任意文件、文件夹、日志判定及父子监控根去重测试，并更新 `Unreleased` 文档
