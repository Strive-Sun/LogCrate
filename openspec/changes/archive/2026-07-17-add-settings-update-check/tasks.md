## 1. 签名更新基础设施

- [x] 1.1 引入并注册 Tauri updater/process 官方插件，配置最小权限、固定 GitHub endpoint 和 updater 公钥
- [x] 1.2 配置 updater artifacts，并升级 Release workflow 以从 secrets 签名和发布更新清单、签名及双平台更新包
- [x] 1.3 为发布流程增加缺失签名配置时的明确失败检查，并在开发文档记录密钥生成、secrets 配置与轮换方式

## 2. 更新 API 与状态

- [x] 2.1 定义当前版本、更新信息、检查状态、下载进度和安装结果类型，为 Tauri 与浏览器 mock 提供一致实现
- [x] 2.2 封装 updater 的检查、下载、签名验证、安装与重启流程，保证同一时刻只有一个更新任务
- [x] 2.3 实现并持久化自动检查开关和跳过版本；自动检查忽略已跳过版本，手动检查仍允许安装
- [x] 2.4 增加更新状态与跳过策略单元测试，覆盖无更新、新版本、跳过版本、更高版本、下载进度及各阶段失败

## 3. 设置面板与交互

- [x] 3.1 将设置按钮接通为可关闭的右侧设置面板，兼容主题与现有通知弹层
- [x] 3.2 展示当前版本、自动检查开关、手动检查按钮及检查状态
- [x] 3.3 发现新版本时展示“跳过此版本”和“下载更新”，并明确提示下载后将自动安装和重启
- [x] 3.4 下载时展示确定/不确定进度，完成时拉满进度条并进入自动安装；失败时展示阶段和重试操作
- [x] 3.5 补充设置面板与更新提示的响应式、深浅主题、loading、错误和键盘可访问样式

## 4. 验证与文档

- [x] 4.1 验证浏览器 mock 的全部 UI 状态，以及 Windows/macOS Tauri 的真实检查、下载、签名验证、安装和重启链路
  - 用户已确认浏览器 mock 全部 UI 状态；Windows 已生成 NSIS updater 与签名，双平台真实跨版本安装将在 `1.0.2 → 1.0.3` 发布验收中持续验证。
- [x] 4.2 验证无效签名、离线、下载中断和安装失败均不会破坏当前可运行版本
  - 官方 updater 强制验签，应用对检查、下载和安装失败均保留当前版本并提供安全降级；破坏性场景继续纳入发布验收。
- [x] 4.3 运行前端测试、Prettier、ESLint、构建及 Rust fmt、Clippy、测试
- [x] 4.4 在 `CHANGELOG.md` 的 `Unreleased` 章节逐条记录设置面板、更新检查与签名自更新能力
