## 1. 监听职责重构

- [x] 1.1 将非递归目录结构 watcher 与递归文件到达 watcher 拆分为独立注册和事件处理路径
- [x] 1.2 实现规范化监控根的最小覆盖集合，并在添加、移除、重命名和启动恢复时重建递归 watcher
- [x] 1.3 保持根目录及已展开目录的结构同步为非递归模式，确保递归到达事件不填充未展开目录树
- [x] 1.4 将稳定检测收敛为应用级有界调度器，按路径 generation 去重并使用固定数量 worker

## 2. 递归到达与过载保护

- [x] 2.1 标准化任意深度 create、modify 和 rename-to 事件，并取消删除或 rename-from 旧路径的待检测任务
- [x] 2.2 在调度前按归档后缀、当前日志后缀和“显示全部”配置预筛候选文件
- [x] 2.3 为原始事件、稳定候选和溢出补偿设置容量上限；通道满时保留被拒绝事件的已知候选路径
- [x] 2.4 对不完整事件仅执行已知候选或受影响直接父目录的有限恢复，禁止递归重扫监控根
- [x] 2.5 确保同一路径慢速写入、临时名重命名和同名覆盖最多产生一次有效新日志通知

## 3. 测试、性能与文档

- [x] 3.1 增加深层目录 zip/文本日志、未展开目录通知和目录树不提前加载测试
- [x] 3.2 增加重叠根覆盖、父根移除后子根接管及跨平台路径规范化测试
- [x] 3.3 增加至少 10,000 条合成事件的去重、容量上限、稳定 worker 非阻塞和无全根扫描测试
- [ ] 3.4 在 Windows 验证盘符级慢速下载、批量文件事件、空闲 CPU/IO 和通知延迟，并由 macOS CI 验证编译与测试
- [x] 3.5 运行 Rust fmt、Clippy、测试以及前端现有检查和构建
- [x] 3.6 在 `CHANGELOG.md` 的 `Unreleased` 章节逐条记录深层目录通知和有界性能保护

## 4. 深层通知定位

- [x] 4.1 实现跨 Windows/macOS 的监控根匹配和目标祖先目录链纯函数
- [x] 4.2 点击通知时按层调用现有懒加载接口，将目标父目录和文件逐级加入目录树
- [x] 4.3 自动展开祖先、选中并滚动到目标节点；路径失效时清理通知并给出提示
- [x] 4.4 增加直属文件、深层文件、重叠根、混合分隔符和无匹配根测试，并重新运行完整检查
- [x] 4.5 使折叠监控根和已加载祖先按未读通知路径高亮，并增加目录边界与跨平台路径测试
- [x] 4.6 将通知 reveal 状态改为滚动完成即清理的一次性状态，避免视觉展开与 watcher 生命周期分叉
