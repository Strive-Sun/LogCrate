## 1. macOS 权限基础设施

- [x] 1.1 确认当前 macOS 构建是否启用 App Sandbox、最低系统版本、现有 bundle identifier 和 entitlement 基线并记录验证结果
- [x] 1.2 增加 macOS 原生系统设置打开命令，优先定位完全磁盘访问权限并实现“隐私与安全性”降级入口
- [x] 1.3 定义可区分权限拒绝、路径不存在、介质离线和普通 I/O 错误的后端错误码与前端类型
- [x] 1.4 为 Windows 和非 macOS 构建提供无行为变化的条件编译实现

## 2. 持久目录授权

- [x] 2.1 将监控配置迁移为兼容旧路径列表的版本化记录，并在 macOS 记录 opaque security-scoped bookmark
- [x] 2.2 在系统目录选择成功后由原生侧创建 bookmark，确保 bookmark bytes 不传入前端业务状态或日志
- [x] 2.3 实现启动时 bookmark 解析、stale 刷新、资源身份校验以及 `startAccessingSecurityScopedResource()` 调用
- [x] 2.4 实现跨 watcher、目录树、搜索和日志查看的访问句柄引用计数及配对释放
- [x] 2.5 为失效书签、权限撤销和旧纯路径配置增加单目录重新授权流程，保留其它监控根正常工作

## 3. 首次启动与恢复 UI

- [x] 3.1 增加版本化的 macOS 首次权限说明弹窗，提供“前往授权”和“稍后”，且引导状态不伪装为授权状态
- [x] 3.2 用户选择前往授权后打开对应系统页面，并展示添加 LogCrate、开启开关、返回重试及必要时重启应用的步骤
- [x] 3.3 在设置或权限状态入口提供再次打开系统页面的操作，不在每次启动或每个文件失败时循环弹窗
- [x] 3.4 在监控根上展示“可访问 / 需要重新授权”等基于实际操作的状态和修复操作
- [x] 3.5 补齐中文、英文文案与无障碍键盘/焦点行为

## 4. 搜索、监控与文件打开集成

- [x] 4.1 启动恢复监控前取得 bookmark 访问句柄，确保 watcher 和惰性目录读取复用同一授权
- [x] 4.2 macOS 搜索 provider 仅处理当前可读或已授权范围，聚合不可读诊断并提供一次性设置入口
- [x] 4.3 双击搜索结果或打开日志时重新校验实际访问结果，按错误类型提示重新授权目录或开启完全磁盘访问权限
- [x] 4.4 确保拒绝或撤销完全磁盘访问权限不阻止应用启动、已授权目录监控和其它搜索结果

## 5. 验证

- [ ] 5.1 增加 bookmark 配置迁移、解析失败、stale 刷新、身份不符、引用计数和错误映射测试
- [ ] 5.2 增加首次引导只自动出现一次、稍后、重新打开设置、深链失败降级和局部重新授权的前端测试
- [ ] 5.3 使用同一个安装构建执行首次授权后连续多次退出和启动，确认不再显示 LogCrate 权限引导或目录选择器
- [ ] 5.4 实机验证权限拒绝/撤销、目录移动、磁盘离线、多个监控根和搜索受保护路径；重新构建、升级与重装后的权限继承不纳入本轮验收
- [ ] 5.5 运行前端测试、Rust 测试、格式检查、clippy 和 macOS bundle 构建，并记录仍需人工确认的系统弹窗行为
