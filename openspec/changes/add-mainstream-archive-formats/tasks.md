## 1. 格式注册与识别

- [x] 1.1 新增统一 `ArchiveFormat` 注册表，覆盖规范名称、复合后缀、magic、单文件流和读取器工厂
- [x] 1.2 将现有 ZIP 与裸文本分派迁移到注册表，并保持现有行为与测试通过
- [x] 1.3 实现 ZIP、7z、RAR、TAR、gzip、bzip2、xz、zstd 文件头检测及压缩 TAR 内层识别
- [x] 1.4 增加伪造后缀、大小写后缀、无后缀和 magic/后缀冲突测试

## 2. TAR 与压缩流

- [x] 2.1 实现 TAR 条目枚举与顺序条目流，只暴露普通文件
- [x] 2.2 实现 `.tar.gz/.tgz` 与单文件 `.gz`，正确合成单文件条目名称
- [x] 2.3 实现 bzip2、xz、zstd 的 TAR 组合与单文件压缩流
- [x] 2.4 为各格式增加空包、嵌套路径、非文本条目、Unicode 文件名和截断流测试

## 3. 7z 与 RAR

- [x] 3.1 评估 7z/RAR 格式专用 crate 与 libarchive，记录维护状态、许可证、原生依赖和包体积
- [x] 3.2 实现未加密单卷 7z 的条目枚举与流式打开
- [x] 3.3 完成 RAR 依赖许可与再分发评审，并记录 RAR4/RAR5 支持矩阵
- [x] 3.4 实现未加密单卷 RAR4/RAR5 的条目枚举与流式打开
- [x] 3.5 对加密、分卷、未知算法和损坏 7z/RAR 返回明确错误

## 4. 嵌套归档

- [x] 4.1 定义稳定嵌套条目链标识，支持跨格式多层路径、选项卡和错误上下文
- [x] 4.2 将归档内受支持格式条目标记为可展开节点，并在展开时验证 magic 后创建下一层读取器
- [x] 4.3 对需要 seek 的嵌套读取器实现流式中间缓存，禁止整文件入内存或写入用户目录
- [x] 4.4 实施默认 5 层深度、整条链累计解码字节上限、取消和缓存生命周期清理
- [x] 4.5 增加同格式嵌套、跨格式嵌套、伪装后缀、深度超限、聚合字节超限和父归档变化测试

## 5. 产品集成

- [x] 5.1 让目录扫描、递归到达 watcher、拖入检查和通知预筛统一使用格式注册表
- [x] 5.2 为所有支持格式提供一致的归档节点、惰性展开、加载状态和失败重试
- [x] 5.3 为单文件压缩流显示一个虚拟日志条目，并允许直接打开和定位
- [x] 5.4 补充中英文格式名称、读取进度、损坏、加密、分卷、嵌套超限和算法不支持错误
- [x] 5.5 更新 README 支持矩阵、使用教程和 CHANGELOG

## 6. 安全、性能与发布验证

- [x] 6.1 对实际解压字节、嵌套深度、嵌套链聚合字节、条目数量、路径长度、扫描字节和扫描时长设置并测试上限
- [x] 6.2 验证目录、链接、设备节点和危险路径不会成为可打开或可落盘条目
- [x] 6.3 验证取消、失败、关闭、LRU 回收与应用重启会清理临时索引、嵌套中间文件和条目清单缓存
- [x] 6.4 使用数百 MB 日志验证固定缓冲读取、首批行增量可读和内存不随条目大小线性增长
- [x] 6.5 运行 Rust fmt/clippy/测试、前端测试/lint/构建及 OpenSpec 严格校验
- [ ] 6.6 在 Windows 与 macOS 构建安装包，记录每平台体积增量并验证原生依赖可随 updater 正常安装
- [ ] 6.7 对每种格式及嵌套组合执行真实文件人工验收：到达通知、逐层展开、打开、滚动、错误与清理
