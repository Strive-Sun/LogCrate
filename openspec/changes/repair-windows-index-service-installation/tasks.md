## 1. 安装与修复可靠性

- [ ] 1.1 让 NSIS 服务安装失败显示阻断性错误并停止成功路径，保持 MSI 失败回滚；构建 Windows 安装包并核对两种产物都包含服务二进制及有效安装动作。
  - **自测标准**：安装钩子静态断言覆盖成功、非零退出和执行失败分支；Windows Tauri 构建生成 MSI/NSIS，产物内容包含版本匹配的 `logcrate_index_service.exe`；失败路径不得只写详情日志后继续成功。
- [ ] 1.2 实现可区分阶段的 Windows 服务错误模型和固定目标/参数的原生 `runas` 重新注册流程，修复后完成服务启动与协议握手。
  - **自测标准**：服务不存在、访问拒绝、启动失败、UAC 取消、修复程序缺失、提升进程失败、pipe 未就绪和协议不兼容均有稳定分类；固定路径/参数不能由前端注入；Rust 定向测试、索引服务编译、格式和 Clippy 通过。
- [ ] 1.3 接入 Tauri/TypeScript 与搜索设置 UI，明确提示 UAC、修复状态和分类错误；成功后只重启一次索引，失败或取消时保留兼容扫描和可重试入口。
  - **自测标准**：前端定向测试覆盖确认修复、成功重建、UAC 取消、各类可读错误、重复点击防护和降级保留；前后端完整 L3 门禁通过；使用 Windows 安装产物验证全新安装可查询服务，删除/损坏服务后点击修复会出现 UAC、恢复服务并通过协议探针。第二台全新 Windows 机器最终复测仍作为发布验收证据。
