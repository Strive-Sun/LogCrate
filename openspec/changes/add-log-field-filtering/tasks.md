## 1. 实现

- [x] 1.1 增加按规范化文件身份自动保存的版本化字段布局持久化工具，覆盖稳定自动布局落盘、拖动释放/重命名确认/类型调整/拆分合并后的原子更新、中间态不落盘、无单独保存按钮、裸文件与归档条目身份、关闭/重启恢复、仅保存布局不保存筛选、结构指纹校验、损坏记录、未知版本、256 项 LRU、“清除已保存布局”和跨文件不复用测试。
- [x] 1.2 增加 Rust 快速/后台两阶段有界抽样、主布局识别、`time`/`level`/`discrete`/`text` 字段类型与边界模型和人工拆分/合并/拖动校正模型，补充级别大小写合并、`WARN`/`WARNING` 独立、普通字段大小写保留、方括号应用日志、Chromium/CEF、Android logcat、混合格式、宽字符、低置信度、交互后边界冻结与资源上限测试。
- [x] 1.3 增加会话级布局/筛选 generation、离散候选值与时间范围统计、未解析集合、后台增量匹配行映射、紧凑筛选窗口与原始窗口匹配标记读取、原始锚点定位/恢复、按结果模式切换关键词查找范围、进度事件和 Tauri/TypeScript API 契约，补充同列 OR、跨列 AND、时间闭区间、高基数降级、紧凑结果顺序、原始行号跳号、高亮模式全量顺序、未解析模式差异、索引增长、编码重建、取消、失败回退、休眠与资源释放测试。
- [x] 1.4 在文件选项卡下方接入与正文横向滚动同步且只包含真实日志字段的字段筛选栏，实现保存布局优先恢复、自动保存状态反馈、后台指纹校验、行号沟槽对齐、字段下拉、单值/多值选择、时间范围、文本筛选、拖动辅助线、重命名、类型调整和拆分/合并；在底部状态栏接入“仅显示匹配/高亮匹配”、未解析开关及禁用说明、清除筛选和布局菜单（重新自动识别/清除已保存布局），并实现窄窗口收纳、紧凑完整行结果、主题化整行匹配背景、模式切换不重扫、两种进度/零结果状态、原始锚点保持/回退、清除后位置恢复、失败状态和键盘可访问性，补充中英文组件及选项卡状态测试。
- [x] 1.5 执行前后端 L3 完整门禁、OpenSpec strict 校验和 Windows Release 真实 Tauri/WebView 验收；分别使用三种截图格式、包含续行/堆栈的混合日志、小文件和不少于 1GiB 的大日志连续运行性能样本，确认全新布局 9/10 ≤300ms 且全部 ≤500ms、保存布局 9/10 ≤100ms 且全部 ≤200ms、后台期间 UI p95 ≤100ms，并验证两种结果模式、主题高亮可辨识、模式切换不重扫、人工修正、筛选布尔语义、横向对齐、原始行号、最新 generation 与资源有界。
- [x] 1.6 将字段筛选栏改为首列覆盖行号区域、全部字段按最长字段理想宽度等宽展示并在可用宽度不足时平均收缩，取消字段栏随正文横向滚动；保留实际解析边界、拖动辅助线、完整字段可访问名称和既有筛选交互，补充宽屏、窄屏、中文标题、横向滚动与动态字段数量组件回归，并完成前端 L2 门禁和 Windows WebView 视觉验收。
- [x] 1.7 将底部状态栏的“仅显示匹配 / 高亮匹配”二段开关改为单个结果模式下拉框，保持默认模式、模式切换语义、未解析开关联动和窄窗口收纳行为不变，补充组件回归并完成前端 L2 门禁。
