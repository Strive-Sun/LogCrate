## 1. 基线与观测

- [ ] 1.1 获取并归档修改前完整应用级 C:/D: 三次基线，记录每阶段耗时、计数和查询结果。
- [ ] 1.2 在真实 Tauri/WebView 中采集搜索输入事件延迟，计算 p95 并验证 ≤100ms。
- [ ] 1.3 增加 provider 总耗时、查询可用、持久化、事件交接和状态收敛的独立指标及报告。

## 2. 生命周期优化

- [ ] 2.1 使用阶段指标定位超过五分钟的瓶颈并实施优化，保持查询可用期间 UI 不阻塞。
- [ ] 2.2 在同一真机和数据快照上重复三轮，验证总耗时、结果数和状态提示一致。

## 3. 单卷恢复

- [ ] 3.1 评估并实现按卷可寻址的 Tantivy 文档字段或 segment 管理。
- [ ] 3.2 增加单卷启动恢复、旧路径删除和回滚测试，目标是完整可搜索 ≤60 秒。

## 4. Windows 切换可靠性

- [ ] 4.1 为 active/staging/previous 切换补充阶段、源/目标路径、句柄和重试诊断。
- [ ] 4.2 在活动索引、并发查询、遗留 `.next`/`.previous` 和外部占用场景下完成真机复现与回归。
- [ ] 4.3 失败时验证旧活动索引仍可查询，并将可定位错误反馈到 UI。

## 5. 验证与发布

- [ ] 5.1 运行 Rust 单元测试、Clippy、格式、前端测试、构建和 OpenSpec 严格校验。
- [ ] 5.2 完成三轮真机测试后更新基准报告与本变更任务状态。
