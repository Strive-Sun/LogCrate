# LogCrate 工作状态

本文件是截至记录时间的现场快照；是否已经闭环必须按 `规则手册/Step-2/工作约束.md`，结合任务文档、验证证据与 Git 历史判断，历史内容不得单独作为未完成依据。

## 1. 当前任务

- 用户目标与当前范围：修复 Windows 搜索索引从 `.next` 切换为 active 时因 Tantivy 后台重载持有文件句柄而报 `os error 5` 的问题。
- 明确不在本轮处理的范围：活动 change `optimize-volume-indexing-followups` 的架构拆分、结构化诊断、真机三轮验证及后续任务。
- change/工作项与当前最小任务编号：独立缺陷修复 `BUG-1`
- 状态：已完成（以本次本地提交成功为生效条件）
- 权威需求、设计与任务文档：本轮用户要求；现行 `openspec/specs/file-search/spec.md`；`规则手册/Step-5/验证规则.md`
- 最近检查点时间：2026-07-27

## 2. 仓库现场

- 分支、基准提交与 `git status` 摘要：`main`，基准 `9c80432`；开始时工作区干净。
- 本任务产生的工作区改动：`src-tauri/src/search_index.rs` 改用手动 reader reload 并增加目录切换回归测试；本状态记录。
- 用户已有或来源不明、必须保留的改动：无。
- 运行中的进程、临时文件或未清理资源：无。
- 最近核对的关键文件：`src-tauri/src/search_index.rs`、`src-tauri/src/search.rs`、Tantivy 0.22.1 reader/watcher 源码、`openspec/specs/file-search/spec.md`、活动 change、`规则手册/Step-0` 至 `Step-5`

## 3. 进度与检查点

- 已完成且可核对的步骤：根因定位、最小修复、回归测试、L3 Rust 完成门禁、首次失败分类和最终差异复核均已完成。
- 正在进行的步骤与准确停止位置：准备精确暂存两个 BUG-1 文件，复核 staged diff 后创建本地 commit。
- 尚未开始的步骤：无本任务实现或验证步骤。
- 下一条可直接执行的操作：暂存 `src-tauri/src/search_index.rs` 与本状态文件，核对后提交 `fix: BUG-1 释放搜索索引切换句柄`。
- 当前阻塞、依赖或待确认事实：活动 change 缺少 `approval.md`，本轮只恢复现有行为，不实施其任务或新增语义。

## 4. 决策与假设

只记录恢复任务必须知道且尚未进入权威设计文档的决定；改变需求或方案的决定必须同步到对应 spec/design/tasks。

| ID | 决策或假设 | 依据 | 影响范围 | 重新评估条件 | 状态 |
|---|---|---|---|---|---|
| D-001 | 执行单位为任务文档中最小编号的叶子任务；无任务文档时当前用户请求为一个单位 | 用户要求以 1.1/1.2 为例逐项推进 | 现 Step-4、Step-5 | 用户指定不同拆分方式 | 已生效 |
| D-002 | 每个执行单位通过门禁后创建本地独立 commit，但不自动 push/tag/archive | 用户要求自动提交且需保护远端状态 | 现 Step-4 | 用户明确扩大自动化权限 | 已生效 |

## 5. 发现即记录

任何可能影响正确性、范围、验收、恢复或后续维护的发现，都必须在发现时立即记录，不能只保存在聊天上下文中，也不能等任务结束后凭记忆补写。

流程：

1. **记录**：先写观察到的事实、位置和证据，再继续调查或修改。
2. **分类**：标记为当前范围、阻塞、方案变化、独立后续或待确认。
3. **处置**：选择当前处理、更新权威文档、请求确认、转为后续事项或仅交接。
4. **跟踪**：状态变化时更新原记录；未处理项在获得明确去向前不得删除。已解决项的结论进入权威文档或验证证据后可以压缩归档。
5. **交接**：暂停或结束任务时，将所有未处理项逐项带入交接结论。

需要记录的内容包括：潜在缺陷、失败或跳过的测试、警告、未验证平台、文档与代码冲突、来源不明改动、技术债、清理事项，以及本轮不处理但后续可能需要处理的问题。

| ID | 发现时间 | 事实与证据 | 影响 | 分类/是否在本轮 | 处置位置或建议 | 状态 |
|---|---|---|---|---|---|---|
| F-001 | 2026-07-27 | `git status --short` 曾显示活动 change 的 4 类文件已有修改 | 自动提交若宽泛暂存会混入无关工作 | 历史现场/不在本轮 | 已由 `9c80432` 恢复并提交；BUG-1 开始时工作区干净 | 已解决 |
| F-002 | 2026-07-27 | `git diff --check` 对现有跟踪文件提示 LF 将转换为 CRLF，退出码为 0 | 当前不影响格式校验，后续可能产生行尾噪声 | 工具警告/不在本轮 | 保留提示，不为本次规则修改扩大范围 | 已接受风险 |
| F-003 | 2026-07-27 | `SearchIndex::commit()` 已同步 `reader.reload()`，同时 `OnCommitWithDelay` 会在提交后启动第二次后台 reload；Tantivy watcher 回调持有 reader/index，可能在对象 drop 后继续占用 `.next` mmap | Windows staging→active 重命名在 21 次重试后仍报 `os error 5` | 当前范围 | reader 改用 `Manual`，保留同步 reload；提交后释放、重命名和重开测试及快照切换回归均通过 | 已解决 |
| F-004 | 2026-07-27 | 活动 change `optimize-volume-indexing-followups` 没有 `approval.md`，且其 5.1/5.2 覆盖更完整的诊断与真机复现 | 不得借本缺陷修复实施未批准的新架构或验收范围 | 独立后续/不在本轮 | 本轮仅做局部句柄修复；后续 change 获批后再按任务顺序推进 | 已转后续 |
| F-005 | 2026-07-27 | 首次 Rust 全量测试中 `volume_scheduler_consumes_results_in_completion_order_and_isolates_failures` 断言首个完成项固定为 `fast`，实际无延迟的 `failed` 先完成；136 通过、1 失败、7 ignored | L3 全量门禁首次未通过；该测试与 reader reload 无调用路径，但存在既有调度竞态断言 | 独立后续/不在本轮 | 有效隔离复跑 10/10 通过，第二次全量 137/137 通过；保留首次失败并建议后续用同步原语替代固定完成顺序假设 | 已转后续 |

状态使用：待确认 / 待处理 / 处理中 / 已转后续 / 已解决 / 已接受风险。

## 6. 验证现场

- 验证等级与选择原因：L3，修改全盘搜索索引与 Windows active/staging 核心切换路径。
- 已通过的命令/人工步骤及证据位置：新增回归 1/1；查询快照切换定向测试 2/2；Rust 格式检查通过；第二次 Rust 全量 137 通过、0 失败、7 ignored；全目标 Clippy 零警告；全目标 `cargo check` 通过。
- 失败、跳过、ignored 或不稳定结果：首次直接调用 `cargo` 因 PATH 缺失未启动，随后使用 `C:\Users\sunjinlei\.cargo\bin\cargo.exe`；首次 Rust 全量 136 通过、1 失败、7 ignored，F-005 隔离复跑 10/10 和第二次全量均通过；一次误加 `--exact` 的 0-test 重复结果已明确作废。7 个 ignored 均为需管理员/索引服务、真实卷、性能脚本或运行时索引的既有专项测试。
- 尚未验证的行为、平台或门禁：未在用户约 489 万文件的真实运行时索引上重建；本轮通过 Windows 本机目录 rename 回归和结构性移除后台 watcher 证明句柄路径。前端、Tauri 产物和性能基准不受本次 reader 策略修复影响，不适用。
- 日志、报告、截图和构建产物位置：无独立产物；命令结果保存在本轮交付上下文。
- 当前小节是否满足进入下一任务的条件：最终差异检查与精确本地 commit 成功后满足。

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| BUG-1 定向/L3 | `cargo test ... committed_index_releases_directory_for_snapshot_switch`；`cargo test ... query_snapshot_switch` | 1/1 与 2/2 通过 | 未使用真实百万文件索引；结构性修复不依赖规模 |
| BUG-1 Rust 完整/L3 | `cargo test`；`cargo fmt --all --check`；`cargo clippy --all-targets -- -D warnings`；`cargo check --all-targets` | 最终全量 137/137 通过、7 ignored；其余零失败/零警告 | 首次独立 flaky 见 F-005；ignored 原因见上 |

## 7. 恢复核对

- [x] 已重新读取规则、当前任务和相关权威资料
- [x] 已核对任务状态、Git 差异、关键文件和验证证据
- [x] 已区分本任务改动、用户已有改动和生成物
- [x] 已核对决策、假设以及全部待确认/未处理发现
- [x] 已确认准确停止位置和下一条操作

## 8. 详细交接结论

### 当前结论

- 当前任务处于什么状态，为什么：BUG-1 实现与 L3 自动化门禁已完成，等待最终差异复核和本地独立提交。
- 已完成的结果：Tantivy reader 改为仅同步手动 reload，消除提交后后台二次 reload 对 `.next` 的句柄占用；新增提交、查询、释放、目录切换和重开回归。
- 未完成的工作及原因：仅剩精确提交；用户真实百万文件索引重建未执行，避免直接改动真实应用数据且结构性句柄路径已由 Windows 回归覆盖。
- 是否允许继续以及前置条件：`git diff --check`、最终 diff、staged diff 和 commit 范围核对通过后允许结束。
- 本小节计划提交信息（实际 commit 在提交后由交付说明报告）：`fix: BUG-1 释放搜索索引切换句柄`

### 不可丢失的上下文

- 关键决定、假设和约束：本轮是恢复现行搜索行为的局部修复，不实施未批准 change 的架构、诊断或真机验收任务。
- 必须保留的工作区改动或运行现场：只提交 `src-tauri/src/search_index.rs` 与本状态文件；开始时无用户已有改动。
- 已验证结论与不能误判为已通过的项目：L3 Rust 门禁已通过；首次全量 flaky、作废的 0-test 命令和未运行的真实百万文件重建不得写成通过。

### 所有观察到但未处理的问题

| 发现 ID | 问题摘要 | 证据/位置 | 影响或风险 | 建议下一步 |
|---|---|---|---|---|
| F-004 | 活动 change 尚无审批记录 | `openspec/changes/optimize-volume-indexing-followups/approval.md` 不存在 | 其结构化诊断、三轮真机复现等任务不得实施 | 维护者批准稳定 revision 后再按 1.1 起顺序推进 |
| F-005 | 卷调度测试对两个立即完成任务作固定先后断言 | `src-tauri/src/search.rs:4427` 首次全量实际先返回 `failed` | 全套并发负载下可能偶发失败 | 独立修复测试同步：显式控制 `fast` 与 `failed` 完成顺序，或只断言 `slow` 不先完成 |

### 下一位执行者的起点

1. 首先核对：读取 Step-0 至 Step-5，检查 BUG-1 commit 和剩余 `git status`。
2. 若继续活动 change：先确认有效 `approval.md`，随后只能从未完成的 1.1 开始。
3. 若处理 F-005：建立独立最小任务，先让测试使用确定性同步，再按相应等级验证和提交。

### 交接完成检查

- [x] 所有观察到的问题都已进入第 5 节，未依赖聊天记忆
- [x] 所有未处理问题都已在交接结论和最终交付说明中逐项列出，或明确写明“未发现”
- [x] 首次失败、未验证项、平台缺口和来源不明改动没有被遗漏
- [x] 当前小节要求的验证全部通过，且不存在本小节范围内的阻断或未分类发现
- [x] 当前小节已更新任务状态，并准备好不包含无关改动的精确提交范围与 commit 信息
- [x] 下一位执行者无需重新询问背景即可找到准确起点、文件和命令
