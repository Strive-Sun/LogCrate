# LogCrate 工作状态

本文件是截至记录时间的现场快照；是否已经闭环必须按 `规则手册/Step-2/工作约束.md`，结合任务文档、验证证据与 Git 历史判断，历史内容不得单独作为未完成依据。

## 1. 当前任务

- 用户目标与当前范围：调整搜索快捷键；顶栏全盘搜索只由入口触发，`Ctrl+F` 打开当前日志的轻量关键词查找弹窗，并支持反向查找、全词匹配、区分大小写和循环查找。
- 明确不在本轮处理的范围：固定查找栏、匹配计数、正则、替换、跨日志查找、历史记录，以及活动 change `optimize-volume-indexing-followups` 的索引优化。
- change/工作项与当前最小任务编号：`update-log-search-shortcut`，任务 1.2
- 状态：任务 1.2 已完成 L3 门禁与勾选，等待独立本地提交
- 权威需求、设计与任务文档：`openspec/changes/update-log-search-shortcut/proposal.md`、`design.md`、`tasks.md`、`specs/file-search/spec.md`、`specs/log-viewing/spec.md`
- 最近检查点时间：2026-07-28

## 2. 仓库现场

- 分支、基准提交与 `git status` 摘要：`main`，任务 1.1 基准 `f5dc9d6`；当前仅有任务 1.2 的 Rust 日志查找、Tauri IPC、前端 API 契约/mock、任务勾选和本状态改动。
- 本任务产生的工作区改动：增加单行受控窗口扫描，支持正反向、全词、Unicode 大小写和单次回绕；返回一个匹配、边界与索引状态；通过 `spawn_blocking` 暴露 Tauri IPC，并同步 TypeScript 与 mock 契约。
- 用户已有或来源不明、必须保留的改动：无。
- 运行中的进程、临时文件或未清理资源：本轮未启动应用进程，未创建临时资源。
- 最近核对的关键文件：`openspec/changes/update-log-search-shortcut/` 全部文档、`openspec/specs/file-search/spec.md`、`openspec/specs/log-viewing/spec.md`、`规则手册/Step-0` 至 `Step-5`、`openspec/AGENTS.md`

## 3. 进度与检查点

- 已完成且可核对的步骤：任务 1.1 已提交为 `f5dc9d6`；任务 1.2 定向 3/3、Rust 全量 143/143（8 项按环境 ignored）、Clippy、fmt、全目标 check、前端 65/65、类型、Lint、格式和生产构建均通过；OpenSpec 严格校验通过。
- 正在进行的步骤与准确停止位置：任务 1.2 已勾选并完成最终差异核对，等待精确暂存和独立提交。
- 尚未开始的步骤：任务 1.3 至 1.4。
- 下一条可直接执行的操作：核对 staged diff 后提交 `feat: 1.2 增加日志窗口化查找 IPC`；提交范围正确后才可进入 1.3。
- 当前阻塞、依赖或待确认事实：无；本小节尚未增加前端弹窗或快捷键分派。

## 4. 决策与假设

只记录恢复任务必须知道且尚未进入权威设计文档的决定；改变需求或方案的决定必须同步到对应 spec/design/tasks。

| ID | 决策或假设 | 依据 | 影响范围 | 重新评估条件 | 状态 |
|---|---|---|---|---|---|
| D-001 | 执行单位为任务文档中最小编号的叶子任务；无任务文档时当前用户请求为一个单位 | 用户要求以 1.1/1.2 为例逐项推进 | 现 Step-4、Step-5 | 用户指定不同拆分方式 | 已生效 |
| D-002 | 每个执行单位通过门禁后创建本地独立 commit，但不自动 push/tag/archive | 用户要求自动提交且需保护远端状态 | 现 Step-4 | 用户明确扩大自动化权限 | 已生效 |

## 5. 发现即记录

任何可能影响正确性、范围、验收、恢复或后续维护的发现，都必须在发现时立即记录，不能只保存在聊天上下文中，也不能等任务结束后凭记忆补写。

流程：

1. **记录**：先写观察到的事实、位置和证据，再继续调查或修改。
2. **分类**：标记为当前范围、阻塞、方案变化、独立后续或待确认。
3. **处置**：选择当前处理、更新权威文档、请求确认、转为后续事项或仅交接。
4. **跟踪**：状态变化时更新原记录；未处理项在获得明确去向前不得删除。已解决项的结论进入权威文档或验证证据后可以压缩归档。
5. **交接**：暂停或结束任务时，将所有未处理项逐项带入交接结论。

需要记录的内容包括：潜在缺陷、失败或跳过的测试、警告、未验证平台、文档与代码冲突、来源不明改动、技术债、清理事项，以及本轮不处理但后续可能需要处理的问题。

| ID | 发现时间 | 事实与证据 | 影响 | 分类/是否在本轮 | 处置位置或建议 | 状态 |
|---|---|---|---|---|---|---|
| F-001 | 2026-07-27 | `git status --short` 曾显示活动 change 的 4 类文件已有修改 | 自动提交若宽泛暂存会混入无关工作 | 历史现场/不在本轮 | 已由 `9c80432` 恢复并提交；BUG-1 开始时工作区干净 | 已解决 |
| F-002 | 2026-07-27 | `git diff --check` 对现有跟踪文件提示 LF 将转换为 CRLF，退出码为 0 | 当前不影响格式校验，后续可能产生行尾噪声 | 工具警告/不在本轮 | 保留提示，不为本次规则修改扩大范围 | 已接受风险 |
| F-003 | 2026-07-27 | `SearchIndex::commit()` 已同步 `reader.reload()`，同时 `OnCommitWithDelay` 会在提交后启动第二次后台 reload；Tantivy watcher 回调持有 reader/index，可能在对象 drop 后继续占用 `.next` mmap | Windows staging→active 重命名在 21 次重试后仍报 `os error 5` | 当前范围 | reader 改用 `Manual`，保留同步 reload；提交后释放、重命名和重开测试及快照切换回归均通过 | 已解决 |
| F-004 | 2026-07-27 | 活动 change `optimize-volume-indexing-followups` 没有 `approval.md`，且其 5.1/5.2 覆盖更完整的诊断与真机复现 | 不得借本缺陷修复实施未批准的新架构或验收范围 | 独立后续/不在本轮 | 本轮仅做局部句柄修复；后续 change 获批后再按任务顺序推进 | 已转后续 |
| F-005 | 2026-07-27 | 首次 Rust 全量测试中 `volume_scheduler_consumes_results_in_completion_order_and_isolates_failures` 断言首个完成项固定为 `fast`，实际无延迟的 `failed` 先完成；136 通过、1 失败、7 ignored | L3 全量门禁首次未通过；该测试与 reader reload 无调用路径，但存在既有调度竞态断言 | 独立后续/不在本轮 | 有效隔离复跑 10/10 通过，第二次全量 137/137 通过；保留首次失败并建议后续用同步原语替代固定完成顺序假设 | 已转后续 |
| F-006 | 2026-07-27 | BUG-1 推送后，用户用仓库 Debug 程序真实复测仍在 `switch-staging-to-active` 经过 21 次尝试后报 `os error 5`；active 与 `.next` 同时存在 | F-003 的后台 reader watcher 解释至多是部分原因，原小规模 rename 回归不足以证明真实大索引切换 | 当前范围 | BUG-2 通过 F-007 根因修复；热重载后真实索引成功切换且查询返回结果 | 已解决 |
| F-007 | 2026-07-27 | 失败 `.next` 的 `meta.json` 时间为 19:13:51，但合并 segment `b577...` 最后写入到 19:14:02；Tantivy 0.22.1 `IndexWriter::drop` 只 kill updater/join 写 worker，而 `wait_merging_threads` 才等待 merge 队列清空 | 后台 merge 在 commit 和普通 drop 后继续持有/写入 `.next`，1 秒重试窗口必然不足且直接 rename 不安全 | 当前范围 | 增加消费式 `SearchIndex::close`：先 drop reader，再 `wait_merging_threads`；staging 和旧 active 切换前均调用 | 已解决 |
| F-008 | 2026-07-27 | 基于失败现场创建 112 文件、931,187,663 字节的 NTFS 硬链接克隆，提交探针文档并触发合并；有序关闭/切换专项测试运行 64.12 秒后通过，克隆与 active 临时目录均已清理 | 证明修复在真实 segment 规模下会等待 merge 后再切换，不再依赖小索引推断 | 当前范围 | 保留显式 ignored 环境测试 `runtime_large_index_waits_for_merges_before_snapshot_switch` 作为生产规模复验入口 | 已解决 |
| F-009 | 2026-07-27 | Sysinternals Handle 与三张窗口诊断截图保存在 `%TEMP%`；精确 `Remove-Item` 清理被执行策略拒绝。首次截图 P/Invoke 因旧 VS98 `LIB` 警告失败，另一次屏幕抓取被前景窗口遮挡；随后 `PrintWindow` 成功 | 不影响仓库、应用或索引；约 2.6MB 临时诊断数据残留 | 独立清理/不在产品范围 | 路径为 `logcrate-handle-diag/`、`logcrate-bug2-window.png`、`logcrate-bug2-printwindow.png`、`logcrate-bug2-query.png`，可由用户或后续允许的清理流程删除 | 已转后续 |
| F-010 | 2026-07-28 | BUG-3 首次定向编译因新增 `Mutex` 未导入失败；命令退出码 1，错误为 `cannot find type Mutex in this scope` | 阻止首次测试，未影响仓库外状态 | 当前范围 | 随后移除不必要的互斥设计，使用不可变状态快照；定向测试与完整 L3 门禁均通过 | 已解决 |
| F-011 | 2026-07-28 | 用户真实复测 `93bda10` 后页面仍长期显示全屏“搜索中”；日志显示 React 1961ms 可交互、搜索 1966ms 已调度，C/D 卷 MFT 正常枚举 | 上一轮只缩短后端状态路径，未消除前端双 IPC 全页门槛，BUG-3 完成结论无效 | 当前范围 | BUG-3R 使用立即快照渲染和独立 IPC 更新；新增两个 IPC 永久 pending 仍显示搜索界面的回归测试 | 已解决，待用户复测 |
| F-012 | 2026-07-28 | BUG-3R 首次完成门禁中 Prettier 报 `FileSearchPanel.test.tsx` 格式不合规；功能测试、类型、ESLint 和构建已通过 | 文档格式门禁未通过 | 当前范围 | 对单个测试文件执行 Prettier，随后格式、类型和 ESLint 复验通过 | 已解决 |
| F-013 | 2026-07-28 | 任务 1.1 首次 `rg` 定位命令因 PowerShell 嵌套引号缺少终止符而退出，未执行搜索 | 仅影响调查命令，无仓库或产品状态变化 | 当前范围 | 将复杂表达式拆为多个简单 `rg` 查询后继续 | 已解决 |
| F-014 | 2026-07-28 | 任务 1.2 首次 Rust 定向命令因当前 PowerShell `PATH` 找不到 `cargo` 而未启动；同一命令后的 TypeScript 检查无输出 | Rust 定向门禁尚未验证，TypeScript 类型检查通过 | 当前范围 | 已定位 `C:\Users\sunjinlei\.cargo\bin\cargo.exe` 并使用绝对路径完成全部 Rust 门禁 | 已解决 |
| F-015 | 2026-07-28 | 任务 1.2 首次格式门禁中 `cargo fmt --check` 要求调整 `index.rs`，Prettier 要求格式化 `mock.ts`；ESLint 与 TypeScript 未报错 | 格式门禁未通过，功能结论不受影响 | 当前范围 | 已只格式化两个受影响文件，最终 Rust/前端格式门禁通过 | 已解决 |
| F-016 | 2026-07-28 | 任务 1.2 首次 L3 完整门禁中 Clippy 报 4 处 `Option::is_none_or` 不兼容项目 Rust 1.70 MSRV；同轮 Rust 全量 143/143、前端 65/65、Lint、格式与构建通过 | Rust Clippy 门禁失败，任务不能完成 | 当前范围 | 已改用 Rust 1.70 兼容的 `map_or`，定向、全量、Clippy、check 与格式复验通过 | 已解决 |
| F-010 | 2026-07-27 | 本机首次 `npm run tauri:build` 因 CLI 子进程找不到 Cargo 退出；补入真实工具链路径后成功生成 `1.0.17` Release 应用、MSI 与 NSIS 安装器，最终因本机按设计未配置 `TAURI_SIGNING_PRIVATE_KEY` 而无法生成 updater 签名 | 本地已验证 Windows Release 编译与安装器打包，但签名和 macOS 通用产物必须由正式 GitHub Actions 完成 | 当前发布门禁 | 保留首次环境失败；远端 `v1.0.16` 发布工作流及签名资产完整，推送 `v1.0.17` 后必须监控双平台工作流、签名、`latest.json` 与 Release 资产全部成功 | 处理中 |

状态使用：待确认 / 待处理 / 处理中 / 已转后续 / 已解决 / 已接受风险。

## 6. 验证现场

- RELEASE-1 验证等级与选择原因：L3，正式版本、安装器、自动更新签名与 GitHub Release 均属于发布边界。
- RELEASE-1 已通过：`release:check`（`v1.0.17`，1 项更新）、`brand:check`、全部 14 项 OpenSpec strict validate、前端 63/63、类型、Lint、Prettier、生产构建、Rust 137/137（8 ignored）、fmt、全目标 Clippy、全目标 check、Windows 索引服务 check；Windows Release 应用、MSI 与 NSIS 均生成且版本为 `1.0.17`。
- RELEASE-1 待远端完成：本机无 updater 私钥且不应保存该私钥；正式工作流需生成 Windows/macOS 签名更新包、`latest.json` 和公开 Release。首次 Cargo PATH 与本地签名门禁证据见 F-010。

- 验证等级与选择原因：L3，修改全盘搜索索引、Tantivy merge 生命周期和 Windows active/staging 核心切换路径。
- 已通过的命令/人工步骤及证据位置：关闭/切换 1/1；查询快照 2/2；失败回滚 1/1；931MB 真实规模克隆专项 1/1（64.12 秒）；Rust 全量 137 通过、0 失败、8 ignored；格式、全目标 Clippy 和全目标编译通过；热重载真实应用成功发布 4,903,834 文件 active 索引并查询 `notepad.exe` 返回 33 项。
- 失败、跳过、ignored 或不稳定结果：BUG-1 用户复测失败见 F-006；BUG-2 没有代码测试失败。句柄工具在失败结束后未找到残留句柄，与 merge 已结束一致。窗口诊断两次无效尝试见 F-009，最终 `PrintWindow` 证据有效。8 个 ignored 中新增 1 个真实规模测试，已通过显式环境命令单独运行；其余为既有管理员/服务/真实卷/性能专项。
- 尚未验证的行为、平台或门禁：未重复三轮全量重建；这是未批准活动 change 5.2 的范围，不属于恢复当前错误的局部 BUG-2。前端代码、Tauri 配置和发布产物未改变。
- 日志、报告、截图和构建产物位置：有效窗口证据 `%TEMP%/logcrate-bug2-printwindow.png`（active 已发布、无错误）与 `%TEMP%/logcrate-bug2-query.png`（33 项结果）；失败现场与测试命令记录在本状态和本轮输出。
- 当前小节是否满足进入下一任务的条件：最终差异、staged diff 与本地 commit 成功后满足。

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| BUG-2 定向/L3 | `cargo test ... closed_index...`；`query_snapshot_switch`；`failed_staging_switch...` | 1/1、2/2、1/1 通过 | 无 |
| BUG-2 真实规模/L3 | 931MB 失败索引硬链接克隆 + `runtime_large_index_waits_for_merges_before_snapshot_switch -- --ignored` | 1/1 通过，64.12 秒，fixture 完整清理 | 未做活动 change 要求的三轮场景矩阵 |
| BUG-2 Rust 完整/L3 | `cargo test`；`cargo fmt --all --check`；`cargo clippy --all-targets -- -D warnings`；`cargo check --all-targets` | 137/137 通过、8 ignored；其余零失败/零警告 | ignored 条件已分类 |
| BUG-2 应用复测/L3 | 热重载真实 C:/D: 索引；UI Automation 查询 `notepad.exe`；`PrintWindow` 核对 | active 唯一存在；4,903,834 文件；33 项结果；3376ms；无切换错误 | 后台 SQLite/USN 持久化继续，不阻塞查询 |

### BUG-3 验证补充

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| BUG-3 定向/L3 | `cargo test ... search_status_and_config_do_not_wait_for_manager_initialization`；`cargo test ... initializing_status_is_available_before_manager_creation` | 各 1/1 通过 | 无 |
| BUG-3 Rust 完整/L3 | `cargo test`；`cargo fmt --all --check`；`cargo clippy --all-targets -- -D warnings`；`cargo check --all-targets` | 140/140 通过、8 ignored；其余零失败/零警告 | 未执行真实 GUI 人工复测；自动化已覆盖阻塞根因 |

### BUG-3R 验证补充

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| BUG-3R 定向/L2 | `npx tsx --test src/components/FileSearchPanel.test.tsx` | 4/4 通过、无 React 警告；永久 pending IPC 场景可立即显示输入框和搜索头部 | 无 |
| BUG-3R 前端完整/L2 | `npm test`；`npx tsc --noEmit`；改动文件 ESLint/Prettier；`npm run build` | 64/64 通过；类型、静态检查、格式和生产构建通过 | 尚待用户真实 GUI 复测 |

### update-log-search-shortcut / 1.1 验证补充

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| 任务 1.1 定向/L2 | `npx tsx --test src/components/SettingsPanel.test.tsx`；生产源码 `rg` 核对 | 4/4 通过；启用入口只有正常按钮激活会调用打开动作；生产源码无 `Ctrl+F` 全盘搜索引用 | 无 |
| 任务 1.1 前端回归/L2 | `npm test`；`npx tsc --noEmit`；改动文件 ESLint/Prettier | 65/65 通过；类型、静态检查和格式均通过 | 未运行生产构建：未改变入口、依赖、构建配置或资源加载 |

### update-log-search-shortcut / 1.2 验证补充

| 范围/等级 | 命令或步骤 | 结果 | 未覆盖项及原因 |
|---|---|---|---|
| 任务 1.2 定向/L3 | `cargo test ... index::tests::search` | 3/3 通过；覆盖正反向、全词、大小写、Unicode、正反向回绕、边界停止、无匹配与索引未完成 | 无 |
| 任务 1.2 Rust 完整/L3 | `cargo test`；`cargo fmt --all --check`；`cargo clippy --all-targets -- -D warnings`；`cargo check --all-targets` | 143/143 通过、8 项按环境 ignored；格式、Clippy 和全目标检查通过 | ignored 为既有管理员、真实索引、性能和真卷专项，本任务不触发其环境条件 |
| 任务 1.2 前端完整/L3 | `npm test`；`npx tsc --noEmit`；`npm run lint`；`npm run format:check`；`npm run build` | 65/65 通过；类型、Lint、格式与生产构建通过 | 前端 UI 属于任务 1.3，当前只验证共享 IPC 契约与 mock 编译 |
| 任务 1.2 OpenSpec/L3 | `openspec validate update-log-search-shortcut --strict`；`git diff --check` | 严格校验和差异格式通过 | 无 |

## 7. 恢复核对

- [x] 已重新读取规则、当前任务和相关权威资料
- [x] 已核对任务状态、Git 差异、关键文件和验证证据
- [x] 已区分本任务改动、用户已有改动和生成物
- [x] 已核对决策、假设以及全部待确认/未处理发现
- [x] 已确认准确停止位置和下一条操作

## 8. 详细交接结论

### update-log-search-shortcut / 1.1 当前结论

- 任务 1.1 已移除 App 级 `Ctrl+F` 全盘搜索监听和中英文快捷键提示；顶栏按钮的启用、禁用及点击切换逻辑保持不变。
- 自动化证据：定向组件测试 4/4、前端完整测试 65/65；改动文件 Prettier、ESLint 和 TypeScript 类型检查通过；生产源码 `rg` 未发现 `Ctrl+F` 全盘搜索引用。
- 首次调查命令的 PowerShell 引号错误已记录为 F-013，未影响仓库状态。
- 未发现任务 1.1 范围内的未处理问题；任务 1.2 必须等待本小节独立提交成功后开始。

### update-log-search-shortcut / 1.2 当前结论

- 任务 1.2 已增加受控单行窗口扫描与 `search_log` IPC，每次返回一个匹配及回绕、边界、已索引行数和索引进行中状态。
- 查找支持正向、反向、全词、区分大小写、Unicode 不区分大小写和单次循环；列位置使用 UTF-16 代码单元，与 JavaScript 字符串切片一致。
- IPC 在阻塞线程执行，不占用异步命令线程；扫描不复制整份正文或全量行索引，每次最多读取现有显示上限内的一行。
- 首次 Cargo PATH、格式和 MSRV Clippy 失败分别记录为 F-014 至 F-016，均已修复并完成最终复验。
- 未发现任务 1.2 范围内的未处理问题；任务 1.3 必须等待本小节独立提交成功后开始。

### BUG-3R 历史交接

### 当前结论

- 当前任务处于什么状态，为什么：BUG-3R 前端降级渲染与自动化门禁已完成，等待精确提交和用户真实 GUI 复测。
- 已完成的结果：搜索页面不再以两个初始化 IPC 全部完成作为渲染前置条件；永久 pending 场景仍立即显示搜索输入框、状态栏和设置入口。
- 未完成的工作及原因：真实应用复测需要用户使用当前提交重新运行开发版或安装包确认；自动化无法替代用户当前索引现场。
- 是否允许继续以及前置条件：最终差异正确后允许提交；用户复测若仍失败，必须保留日志并继续恢复同一 BUG-3R，不得再次标记完成。
- 本小节计划提交信息（实际 commit 在提交后由交付说明报告）：`fix: BUG-3R 搜索页面初始化不再全屏阻塞`

### 不可丢失的上下文

- 关键决定、假设和约束：即使状态或配置 IPC 永久延迟，搜索页面也必须先提供可交互外壳；后续真实响应增量替换初始快照。
- 必须保留的工作区改动或运行现场：只提交 `src/components/FileSearchPanel.tsx`、对应测试和本状态文件；不改动用户索引或搜索数据库。
- 已验证结论与不能误判为已通过的项目：自动化已验证永久 pending；尚未把用户截图现场误写为真机通过。

### 所有观察到但未处理的问题

| 发现 ID | 问题摘要 | 证据/位置 | 影响或风险 | 建议下一步 |
|---|---|---|---|---|
| F-004 | 活动 change 尚无审批记录 | `openspec/changes/optimize-volume-indexing-followups/approval.md` 不存在 | 其结构化诊断、三轮真机复现等任务不得实施 | 维护者批准稳定 revision 后再按 1.1 起顺序推进 |
| F-005 | 卷调度测试对两个立即完成任务作固定先后断言 | `src-tauri/src/search.rs:4427` 首次全量实际先返回 `failed` | 全套并发负载下可能偶发失败 | 独立修复测试同步：显式控制 `fast` 与 `failed` 完成顺序，或只断言 `slow` 不先完成 |
| F-009 | 临时诊断工具与截图未清理 | `%TEMP%/logcrate-handle-diag` 与三张 `logcrate-bug2-*.png` | 约 2.6MB 临时空间 | 在允许删除时清理，不得影响真实索引目录 |

### 下一位执行者的起点

1. 首先核对：读取 Step-0 至 Step-5，检查 BUG-3R commit、用户复测结果和剩余 `git status`。
2. 若用户复测通过：将 BUG-3R 视为闭环，不额外创建 bookkeeping commit。
3. 若用户复测仍失败：保留新截图和前端控制台/后端日志，恢复 BUG-3R 并定位渲染结果是否来自最新前端 bundle。

### 交接完成检查

- [x] 所有观察到的问题都已进入第 5 节，未依赖聊天记忆
- [x] 所有未处理问题都已在交接结论和最终交付说明中逐项列出，或明确写明“未发现”
- [x] 首次失败、未验证项、平台缺口和来源不明改动没有被遗漏
- [x] 当前小节要求的验证全部通过，且不存在本小节范围内的阻断或未分类发现
- [x] 当前小节已更新任务状态，并准备好不包含无关改动的精确提交范围与 commit 信息
- [x] 下一位执行者无需重新询问背景即可找到准确起点、文件和命令
