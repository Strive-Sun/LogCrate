# 更新日志

本文档记录 LogCrate 每个正式版本的重要变更。发布说明按版本维护，并由 Release 工作流自动提取到 GitHub Releases。

## [Unreleased]

尚未发布的变更先记录在此；发布时移动到对应版本章节。

## [1.0.33] - 2026-08-17

### 优化

- 全盘文件搜索采用固定 4 worker、8 项运行窗口和按卷 SQLite 暂存，在 1、4、5、10、64 个卷范围下保持重资源上限不随卷数增长。
- 多卷索引使用稳定卷身份隔离盘符变化与复用，查询快照达到 `ready` 后再于后台完成持久化和事件交接，避免耐久写入阻塞可搜索结果。
- 优化真实多卷 MFT 路径解析和查询发布，C:/D: 约 580 万条记录的首批结果中位数降至 175ms、查询就绪中位数降至 47.8 秒，索引期间输入响应 p95 为 17.5ms。

### 修复

- 修复四个索引服务连接占满 named pipe 后额外客户端无法收到忙响应并可能终止服务的问题；容量已满时现在稳定返回可重试 429 并继续监听。
- 修复单卷服务、协议、暂时离线或后台持久化失败影响其它卷完成的问题；可恢复故障会按卷持久重试，明确的离线、权限拒绝或设备损坏进入需关注状态且不再永久显示索引动画。
- 搜索结果列表使用不透明主题背景，浅色主题下保持纯白，不再透出桌面或底层窗口内容。
- 修复 Windows 索引恢复调度在 macOS 构建中缺少条件编译边界的问题，并同步隔离仅 Windows 使用的调度器导入。

### 工程质量

- 补充多卷资源上限、组合故障、跨启动恢复、外部阻塞、真实 Windows named pipe、盘符复用、Release 性能和 WebView 响应矩阵，并归档对应 OpenSpec change。
- 稳定 macOS 原生目录 watcher 的结构优先回归，允许在固定总预算内忽略目标事件前的无关 FSEvents 批次，同时继续验证稳定检测不得抢先。
- 修复跨平台 Release draft 的并行创建竞争，串行上传 Windows/macOS 资产以确保 updater 清单包含完整平台集合。

## [1.0.29] - 2026-08-14

### 新增

- 新增 DOCX 图文预览，可按文档顺序显示段落、标题、列表、PNG/JPEG 图片和真实表格，并支持合并单元格、分页虚拟化与正文查找。

### 优化

- DOCX 多级列表与标题编号现在解释文档中的编号模板、级别覆盖、样式继承、起始值和重启规则，精确还原 `1.1`、`2.` 等真实段落号。
- 全盘文件搜索改为默认启用，并在应用核心状态就绪后于后台初始化；用户显式关闭后仍保持关闭，不创建搜索存储。

### 修复

- 搜索索引数据库损坏时隔离 SQLite、WAL 和 SHM 文件并自动重建，避免损坏状态或缺失表结构持续阻断搜索。
- 修正搜索偏好写入失败后的回滚测试，使断言与默认启用语义一致并继续验证原值恢复。

### 工程质量

- 补充 DOCX 格式校验、图片资源上限、结构化段落与表格、精确多级编号、查找和动态虚拟化回归，并完成 Windows 与 macOS 代表文档验收。

## [1.0.28] - 2026-08-12

### 优化

- 本地文件搜索在首次启动时默认启用，并在应用核心状态就绪后立即于后台初始化，不再等待延迟任务交接。

### 修复

- 搜索索引数据库损坏时自动隔离 SQLite、WAL 和 SHM 缓存，重新初始化数据库结构并重建 Tantivy 查询索引，避免持续显示数据库损坏或缺少 `metadata` 表错误。
- 搜索数据库在后台扫描或持久化期间发生损坏时自动停止当前操作并启动一次完整重建，原始用户文件不受影响。

## [1.0.27] - 2026-08-11

### 优化

- AI 供应商配置改为分组卡片，清晰展示端点、模型、协议、密钥状态和操作，并用独立状态卡反馈保存或连接测试结果。
- 打开 AI 工作区时保持主窗口原有位置，并始终在右侧增加完整的 440px 分析区域；即使窗口靠近屏幕边缘，也不再自动移动或缩窄 AI 页面。

### 修复

- 修复从主窗口左边缘向左或向右缩放时 AI 页面宽度跟随变化的问题；左边缘现在只调整主页面，右边缘仍只调整 AI 页面。

## [1.0.26] - 2026-08-07

### 新增

- 在日志右键菜单增加“添加到 AI 对话框”，可将任意日志选项卡的选区暂存到当前 AI 会话，由用户确认后再发送。
- AI 会话菜单统一提供“新对话”和“历史记录”；新对话草稿可跨选项卡收集日志，并在首次成功分析后建立加密历史。

### 优化

- 已有 AI 会话再次执行“AI 分析”时，会把当前选区直接加入同一会话并自动发送；空问题会补充明确的日志分析意图。
- AI 会话状态提升到应用级，切换或关闭日志来源选项卡不会丢失当前对话、待发送片段或流式回复。

### 修复

- 修复由空白新对话草稿建立的会话无法继续补充日志，并提示必须重新选择原始日志的问题。
- 修复 AI 等待回复时重复显示两个助手头像，以及全盘搜索保活层遮挡顶栏、导致无法切回监控目录的问题。

## [1.0.25] - 2026-08-06

### 新增

- 在日志选区右键菜单和主工具栏提供 AI 日志分析入口，支持安全配置 OpenAI Chat Completions、Responses 及兼容端点，并为内网 HTTP 目标提供逐供应商显式授权。
- 增加唯一主窗口内的右侧 AI 工作区，支持结构化 Markdown 分析、连续追问、即时消息占位和当前显示器工作区内的 360–440px 自适应展开与关闭恢复。
- 支持选择最多 5 个有界文本或日志附件补充分析，并将对话、选区和附件上下文使用系统密钥链主密钥加密保存到本地历史，提供恢复、单条删除和清空操作。

### 优化

- Responses 现在真正增量读取有界 SSE，并通过请求级 Channel 顺序展示首字与后续内容；并发请求彼此隔离，成功终值用于校准完整正文。
- AI 请求分别执行 10 秒连接、30 秒流空闲和 120 秒总超时，显示响应头、首个内容、流接收和总耗时，同时只保留脱敏错误类别、失败阶段和有界元数据。
- AI 回复按事件顺序生成更紧凑的日志概览、证据、说明和结论；工作区历史卡片、附件标签、composer 与等待动画在窄栏中保持清晰可操作。
- 改进 Chromium/CEF 日志布局识别，支持毫秒时间、`VERBOSE` 数字后缀、正文附加方括号和已保存布局的解析语义复用。

### 修复

- 修复复制日志选区时选择状态丢失、AI 面板关闭后主窗口尺寸偏移，以及展开 AI 后设置或通知弹层落到工作区后方的问题。
- 修复 Responses 成功响应为 SSE、纯文本或兼容 JSON 时被误报为无效响应，以及请求缺少兼容会话头、流式声明或安全诊断信息的问题。
- 修复 AI 历史删除确认弹窗按钮文字不可见、空会话输入框造成误导，以及旧独立 AI 窗口路径残留的问题。

### 工程质量

- Rust 边界强制限制历史角色、消息数、问题长度、历史正文、选区、附件和 4 MiB 响应累计大小，部分流式结果不会保存到历史或进入后续上下文。
- 补充任意 SSE 分块、LF/CRLF、多行 `data:`、心跳、断流、三类超时、Channel 关闭、并发隔离、加密历史损坏、容量和无明文回退测试。

## [1.0.24] - 2026-08-02

### 新增

- 界面模板增加“森绿”风格，以森林绿和薄荷绿呈现自然渐变、清晰边界与克制阴影，同时支持深色、浅色配色和重启恢复。

### 优化

- 设置页的四套模板改为更易浏览的双列卡片布局，窄窗口下自动切换为单列，并保持键盘选择与减少动态效果支持。

## [1.0.23] - 2026-07-31

### 修复

- 修复 Windows NTFS 索引失败后错误降级为兼容扫描的问题，失败范围现在会保留明确的错误状态。

### 工程质量

- 增加索引操作、范围生命周期和快照切换的可观测性与可靠性验证，并完成真实 WebView 搜索输入延迟采样。
- 增加按范围隔离和可回滚快照切换的验证，避免重建索引时遗留旧范围结果。

## [1.0.22] - 2026-07-31

### 优化

- Windows 客户端检查更新时优先访问 Cloudflare Pages，并在镜像不可用时自动回退到官方 GitHub Releases；macOS 更新包继续由 GitHub 提供。

### 工程质量

- 发布流程增加 Pages 镜像的完整资产校验、安全回退部署、缓存策略与公网 SHA-256 验证，并继续使用现有 updater 签名公钥验证安装包。

## [1.0.21] - 2026-07-30

### 新增

- 时间字段筛选改为可点击的日期时间选择器，通过月历、小时和分钟选项填写边界；条件精确到分钟，不包含秒或毫秒，并完整包含结束分钟内的日志。
- 设置页增加“原生”“极光”“琥珀”三套界面模板，支持深浅配色、即时切换和重启恢复。
- Windows 索引服务缺失或损坏时可从设置页主动发起 UAC 修复，重新注册、启动并完成协议握手。

### 优化

- 重新设计字段筛选、字段重命名、类型调整、拆分与合并界面，以更清晰的卡片、状态和操作层级展示可选项。
- 为极光和琥珀模板分别补充日志选项卡、字段筛选栏、筛选弹层及常用控件的专属视觉风格，同时保持原生模板布局不变。
- 限制字段筛选弹层的视口尺寸，并让右侧字段向左展开，避免正文区域的下拉框被窗口边界截断。

### 修复

- Windows NSIS 安装器在索引服务安装失败时中止成功路径并显示明确错误；MSI 继续保持失败回滚。
- 索引服务修复现在区分服务缺失、访问拒绝、启动失败、UAC 取消、程序缺失、协议不兼容和 IPC 未就绪等状态，失败时保留兼容扫描和重试入口。

### 工程质量

- 增加分钟级时间闭区间、三套模板持久化与可访问性、字段弹层防截断、服务错误分类和安装器服务动作回归。

## [1.0.20] - 2026-07-29

### 新增

- 新增按当前日志稳定布局自动生成的字段筛选栏，支持方括号应用日志、Chromium/CEF 和 Android logcat 等常见格式，并可按时间范围、离散值或文本内容组合筛选。
- 支持拖动字段边界、重命名、调整字段类型以及拆分或合并字段；有效布局按文件身份自动保存，关闭选项卡或重启应用后可恢复。
- 字段筛选结果提供“仅显示匹配”和“高亮匹配”两种模式，保留原始行号、未解析行控制、筛选前滚动锚点和当前日志关键词查找范围。
- 当前日志关键词查找弹窗打开时会高亮可见范围内的全部匹配片段，并使用不同样式标识当前导航命中。

### 优化

- 字段筛选栏改为从行号区域开始的等宽布局，按最长字段确定理想宽度，并在空间不足时平均收缩；正文横向滚动不再带动字段栏。
- 底部状态栏将结果模式改为单个下拉框，统一并放宽编码、结果模式、未解析、清除筛选和布局操作的宽度与间距。
- 字段布局识别和筛选采用有界抽样、后台增量扫描及窗口化结果，在大日志中保持界面可交互并避免加载完整正文。

### 修复

- 修复字段筛选栏与正文边界错位、短字段标题被遮挡，以及桌面宽度下底栏筛选操作因折叠状态不可见的问题。
- 修复快速连续修改字段条件或结果模式时旧 generation 的正文缓存可能覆盖最新筛选结果的问题。

### 工程质量

- 增加字段布局识别、布局持久化、筛选布尔语义、未解析行、结果模式、缓存竞态、关键词高亮和 Windows WebView 性能与视觉回归。

## [1.0.19] - 2026-07-28

### 新增

- 增加应用单实例启动锁，重复启动时恢复并聚焦已有 LogCrate 窗口，避免多个进程同时运行。
- 在活动日志中使用 `Ctrl+F` 打开轻量关键词查找弹窗，支持反向、全词、区分大小写和循环查找，并定位、高亮当前匹配。

### 优化

- 全盘文件搜索改为仅通过顶栏“搜索”入口打开，将 `Ctrl+F` 保留给当前日志内容查找。

### 修复

- 修复全盘搜索页面等待后端索引初始化时长时间停留在全屏“搜索中”的问题，页面外壳和输入框现在会立即可用。

### 工程质量

- 收紧 Windows 专用搜索辅助代码的条件编译边界，恢复 macOS 全目标 Clippy、编译和测试门禁。

## [1.0.17] - 2026-07-27

### 修复

- 修复大规模全盘搜索索引仍在后台合并时提前切换快照，导致 Windows 报 `os error 5` 且新索引无法生效的问题。

## [1.0.16] - 2026-07-27

### 优化

- 并行调度多个 NTFS 卷的 MFT/USN 索引任务，按卷发布可搜索结果并提供完整阶段耗时。
- 改进索引状态、发现记录数、可搜索文件数和搜索结果的 UI 展示，避免后台持久化阻塞查询。

### 修复

- 增强索引服务取消、恢复、目录变化和共享索引写入的稳定性，隔离单卷失败并保留可查询快照。

### 工程质量

- 增加多卷索引、增量恢复、查询快照切换和前后端状态聚合测试，并完成大规模真机验证。
- 记录后续索引生命周期优化、真实 WebView 输入延迟和 Windows 索引切换诊断需求。

## [1.0.15] - 2026-07-24

### 新增

- 设置中新增“搜索本地文件”开关，默认关闭；每次启用或关闭都会明确提示在下次启动时生效。

### 优化

- 将主页面首帧与目录监控、搜索、托盘和缓存维护解耦，Windows 日常启动约三秒进入可交互页面，并保留立即可见的原生进度反馈。
- 搜索页面改为保活显示，往返监控目录和搜索页面时保留查询条件与结果，不再重复初始化搜索模块。
- 搜索模块在主页面可交互后才于后台初始化，搜索关闭时不加载索引运行时，减少默认启动资源占用。
- 统一原生启动页与 WebView 加载页的点阵品牌字形和布局，消除启动交接时的字体跳变。
- 清理开发期 `logpeek` 配置、缓存、内部标识和临时文件兼容逻辑，全部统一为 LogCrate。

### 修复

- 修复 macOS 关闭主窗口后点击 Dock 中的 LogCrate 图标无法恢复窗口的问题。
- 修复搜索入口在启用状态下不能正确往返搜索页面与监控目录的问题；未启用时入口保持不可点击并显示设置提示。

### 工程质量

- 默认 `tauri:dev` 改为加载独立预构建静态 bundle，绕过 Vite 冷转换和多模块请求瀑布；需要热更新时仍可使用 `tauri:dev:hmr`。
- 增加完整启动阶段计时、十次发布版基准和 OpenSpec 根因分析，覆盖原生首帧、WebView 导航、React 挂载、可交互帧与后台任务门控。

## [1.0.14] - 2026-07-23

### 新增

- macOS 首次启动新增文件访问说明，可由用户主动打开“完全磁盘访问权限”系统设置，并可在应用设置中再次进入。
- macOS 监控目录新增 security-scoped bookmark 持久授权，支持跨重启恢复访问以及对单个失效目录重新授权。

### 优化

- 搜索、目录监控和日志打开共享持久授权状态；受保护目录不可读时提供明确修复入口，不再循环弹出授权窗口。

### 工程质量

- 新增 macOS 文件访问错误分类、旧监控配置迁移和首次权限引导测试，并保持 Windows 构建行为不变。
- 收紧 Windows NTFS 索引辅助代码的条件编译范围，修复 macOS 测试目标因未使用代码警告导致的 CI 失败。

## [1.0.13] - 2026-07-23

### 修复

- 将 NTFS 索引服务二进制限制为仅在 Windows Tauri 构建中启用，修复 macOS 通用安装包因尝试复制 Windows 专用服务而打包失败的问题。
- 清理搜索模块的平台条件编译边界，避免 macOS release 构建产生仅供 Windows 使用的未引用代码警告。

### 工程质量

- CI 新增 Windows 索引服务 feature 的独立编译检查，防止平台专用二进制从常规跨平台检查中遗漏。

## [1.0.12] - 2026-07-23

### 新增

- 新增全局本地文件搜索，可按文件名、路径、类型和扩展名筛选，并在索引仍在建立时持续展示来自多个磁盘卷的可用结果。
- 搜索结果支持双击打开日志或归档进行预览，并可通过右键菜单将文件所在目录加入 LogCrate 监控。
- Windows NTFS 固定卷新增基于 MFT 与 USN Journal 的快速索引服务，同时提供逐卷 provider 状态、降级原因和服务修复入口。

### 优化

- 搜索热路径迁移到 Orange 衍生的 Tantivy 架构，支持中文、拼音、多词 AND 查询、稳定相关性排序和后台增量提交。
- 全量重建采用原子索引快照切换，构建期间保留旧结果可查，并能在切换中断后恢复上一份完整索引。
- 可见搜索结果的大小、修改时间、可读性和内容类型由有限后台任务按需补齐，避免阻塞 UI 线程。

### 工程质量

- 为 Windows 索引服务增加受限 named-pipe IPC、并发与消息上限、断线取消、MFT/USN 边界测试及安全威胁模型。
- MSI 与 NSIS 安装包接入索引服务安装、修复和卸载配置，并随包分发 Orange GPLv3 与 UnRAR 许可证说明。
- 新增搜索面板交互测试，覆盖查询、双击打开、右键加入监控及失效结果处理。

## [1.0.11] - 2026-07-22

### 修复

- 修复 macOS 上 RAR 路径宽字符指针的符号位类型不匹配，导致通用 Apple 构建无法编译的问题。

## [1.0.10] - 2026-07-22

### 新增

- 启动时立即在原生主窗口显示动态进度条，WebView 完成加载后在同一窗口内无缝接管，消除冷启动期间的白屏。
- 持久化已打开选项卡、显示与折叠顺序及活动项，应用重启后仅立即加载活动日志，其余选项卡按需恢复。
- 已打开的裸日志或归档在外部修改时提示是否重新加载；外部删除时保留当前只读缓存，并可将当前日志内容另存为本地文件。

### 优化

- 将业务状态、缓存准备与目录 watcher 恢复移到后台执行，前端命令异步等待就绪，避免阻塞 UI 线程。
- 移除启动交接后会话恢复状态的大号刷新符号，仅在打开失败时保留警告提示。

### 修复

- 修复目录树中选中、定位与未读状态叠加时同时出现多行背景色的问题。
- 修复选项卡键盘焦点框只包裹标题文字、未覆盖完整标签的问题。

## [1.0.9] - 2026-07-20

### 修复

- 修复小日志索引完成事件被首次订阅消费后无法重放，导致 React StrictMode 或组件重新订阅时进度条长期停留在 0% 的问题；普通日志与压缩包条目现在使用准确的索引文案。

### 新增

- 新增 7z、单卷 RAR4/RAR5、TAR、tar.gz/bz2/xz/zst 与 gzip/bzip2/xz/zstd 单文件压缩流的只读查看支持。
- 支持跨格式嵌套归档惰性展开：只有用户主动展开当前节点时才流式读取下一层，并限制最大深度、累计解码字节、条目数量和危险路径。

### 文档

- 在中英文 README 增加自动适配 GitHub 深浅主题的主界面截图，展示监控目录、ZIP 展开、多选项卡和日志正文。
- 新建 `resources/icons/app` 与 `resources/screenshots` 分类管理图标和产品截图，并同步桌面构建与文档引用路径。
- 精简中英文 README 的项目介绍，聚焦当前产品名称与实际功能。
- 在长期路线图中新增 AI 辅助日志分析方向，并明确选区分析、模型配置与发送前脱敏边界。

### 测试

- 增加可注入的归档安全限制与自动化边界测试，覆盖嵌套深度和累计字节、扫描预算、危险路径、链接/设备节点及缓存清理。
- 增加可重复执行的 Windows release 性能基线，记录 256 MiB 裸日志、ZIP、tar.gz、tar.zst 和跨格式嵌套归档的首批行延迟、吞吐、CPU、内存及磁盘缓存。

## [1.0.8] - 2026-07-20

### 优化

- 将当前仓库、下载入口、状态徽章和 Actions 链接迁移到 `Strive-Sun/LogCrate`，统一产品与 GitHub 项目名称。
- 新构建直接使用 LogCrate 仓库 updater 地址，并保持现有签名公钥不变。

## [1.0.7] - 2026-07-20

### 新增

- 新增由 SVG 母版生成的归档箱与日志行品牌图标，统一用于 README、任务栏、托盘和安装包。
- 新增简体中文与英文应用界面，首次启动跟随系统语言，并可在设置中即时切换和持久化选择。
- 语言切换覆盖目录、右键菜单、日志状态、通知、更新、确认提示、可访问性标签和系统托盘，且不会重置已打开日志。

## [1.0.6] - 2026-07-20

### 新增

- 打开多个裸日志或压缩包日志条目时创建独立选项卡，并分别保留正文滚动位置、索引进度、编码和有限行缓存。
- 正文宽度不足时将溢出选项卡收入带数量提示的“更多”折叠栏；选择折叠项会替换最右侧可见选项卡，并保持稳定顺序。

### 优化

- 鼠标悬停在正文顶部已打开文件名时显示磁盘绝对路径；压缩包日志同时显示压缩包绝对路径和包内路径。
- 后端继续限制最多 5 个活动日志会话；超出上限的选项卡进入休眠，重新激活时透明恢复会话与用户编码选择，避免资源随选项卡数量无限增长。
- 目录树、日志正文和可滚动菜单默认隐藏滚动条，滚动时立即显示，并在停止操作后延迟渐隐。

## [1.0.5] - 2026-07-18

### 新增

- 支持从系统文件管理器向主窗口拖入单个任意文件或文件夹并静默加入监控：文件使用父目录、文件夹使用自身作为监控根，不显示确认弹窗或全窗口拖入提示，原路径不会被复制或移动。
- 拖入 `.log`、`.txt`、`.json` 等可识别文本日志后立即打开正文；父目录已受监控或自动添加成功时，目录树会逐层展开并定位该文件。

### 优化

- 监控根按规范化路径和父子覆盖关系去重；新增父级根时自动归并已有子级根，避免同一目录树被重复监控。

### 修复

- 修复当前查看文件被后缀筛选隐藏时定位状态无法结束，导致重新展开监控根会误展开第一个子目录的问题。

## [1.0.4] - 2026-07-17

### 新增

- 点击主窗口关闭按钮时隐藏到系统托盘，目录监控和后台任务继续运行。
- 系统托盘提供“显示主窗口”和“退出 LogCrate”操作，只有显式退出才会结束应用进程。
- 监控根目录任意深度出现新的 zip 或匹配后缀日志文件时，即使对应子目录尚未展开也会触发新日志提示。
- 点击深层目录的新日志通知时，目录树会逐层展开目标路径、选中文件并滚动到可见区域。
- 深层新日志尚未加载到目录树时，折叠的监控根和已加载祖先仍会显示未读高亮。

### 优化

- 递归到达监听采用重叠根合并、后缀预筛、路径去重、固定 worker 和有界队列，避免空闲轮询及事件溢出时全盘重扫。

### 修复

- 将删除目录和删除文件的确认操作改为应用内弹窗，避免 WebView 原生确认窗口未显示时直接执行危险操作。
- 修复通知定位状态残留导致目录视觉上已展开、后台 watcher 却已释放，从而漏掉后续目录树实时更新的问题。

## [1.0.3] - 2026-07-17

### 新增

- 监控目录中的文件与文件夹在外部创建、删除、重命名或修改后，左侧目录树会实时同步；子目录支持展开时按需加载和监听。

### 修复

- 删除或重命名当前查看的文件时，自动清理失效通知和旧路径视图，避免目录树残留过期内容。

### 优化

- 目录事件按短时间窗口合并并仅更新受影响目录，稳定检测改为独立有界后台队列，避免批量文件操作触发重复全量扫描。

## [1.0.2] - 2026-07-17

### 新增

- 新增设置面板，展示当前应用版本并支持启动自动检查和手动检查更新。
- 发现新版本时支持跳过指定版本，或查看下载进度后自动验证、安装并重启应用。

### 工程质量

- Release 流程增加 Tauri updater 更新清单、双平台更新包和强制签名校验。

## [1.0.1] - 2026-07-17

### 优化

- 将应用版本号统一为 `src-tauri/Cargo.toml` 中的单一人工维护来源。
- GitHub Release 自动使用当前版本的逐条更新内容作为发行说明。

### 工程质量

- 新增发布日志校验，确保 Cargo 版本、Git tag 和 CHANGELOG 版本一致。
- 在 CI 中强制检查每个版本至少包含一条具体更新，拒绝模糊占位内容。
- 补充标准发布步骤，明确版本号、更新日志、检查、提交和 tag 的操作顺序。

## [1.0.0] - 2026-07-17

### 新增

- 支持同时监控多个本地目录，并在应用重启后恢复监控配置。
- 支持识别 zip 日志归档和裸文本日志，在目录树中按需展开归档条目。
- 支持无需手动解压直接查看归档内日志，并通过虚拟滚动浏览大文件。
- 支持后台增量建立行索引，实时展示进度并在索引过程中读取已就绪内容。
- 支持 UTF-8、GBK、GB18030、UTF-16LE 和 UTF-16BE 编码检测与手动切换。
- 支持 LF、CRLF、空行、超长行截断以及末行无换行符的日志内容。
- 支持日志文件和监控目录的重命名、定位、移除监控及删除到系统回收站。
- 支持后缀筛选、新日志通知、未读高亮、深浅主题和可拖拽目录栏。

### 优化

- zip Deflate 条目改为固定缓冲流式读取，避免大条目完整载入内存。
- zip Stored 条目支持受条目边界约束的字节 seek。
- 查看会话支持关闭、取消后台任务和 LRU 回收，自动清理临时缓存。
- 编码切换在后台重建行索引，并通过 generation 防止旧任务覆盖最新选择。

### 修复

- 修复打开失败后界面停留在“打开中”的问题。
- 修复重命名或删除文件后当前视图残留旧状态的问题。
- 修复前后端后缀筛选规则大小写及启动状态不一致的问题。
- 修复加密 zip 条目在目录枚举阶段无法作为元数据展示的问题。

### 工程质量

- 增加 20 个 Rust 单元测试，覆盖归档读取、目录监控、行索引、编码和资源清理。
- 建立 Rust fmt、Clippy、Cargo test、Prettier、ESLint 和前端构建的跨平台 CI 检查。
- 建立 Windows 与 macOS 安装包的 GitHub Release 自动构建流程。
